export default {
  mainInput: {
    default: 'Enter a valid field',
    isNumber: `The code doesn’t look correct, try again it violates a field rule`,
    notEmpty: `The code doesn’t look correct, it violates a form rule`
  },
  secondInput: {
    default: 'Enter a valid field',
    isNumber: `The code doesn’t look correct, try again it violates a field rule`,
    notEmpty: `The code doesn’t look correct, it violates a form rule`
  }
}
